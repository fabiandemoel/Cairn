# Append-only pin of record for CBS source snapshots. Populated by
# `ingestion.cbs_pipeline` on each real ingest (final, unconditional step).
# No snapshots are pinned yet: run the pipeline with R2 credentials to ingest
# the current release, upload the immutable raw file, and append the first
# snapshot here. See README "R2 setup".
source: cbs
dataset: 85669NED
snapshots: []
